{
  "name": "hexsnake",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "Snake on a hexagonal grid with roguelike progression.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.3",
    "@types/node": "^20.16.0",
    "puppeteer-core": "^25.2.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "@tauri-apps/plugin-process": "^2.3.1"
  }
}
